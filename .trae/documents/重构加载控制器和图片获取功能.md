## 重构计划

### 1. 重命名文件和类

* 将 `backend/controller/load/material_controller.py` 重命名为 `backend/controller/load/load_controller.py`

* 将类名 `MaterialLoadController` 改为 `LoadController`

* 更新 `app.py` 中的导入语句

### 2. 迁移 `get_to_ps_images` 方法

* 将 `CoverController` 中的 `get_to_ps_images` 方法迁移到 `LoadController`

* 实现 `LoadController` 的 `get_to_ps_images` 方法，获取 `data/toPs` 目录下的图片列表

* 从 `CoverController` 中移除 `get_to_ps_images` 方法

* 更新 `CoverController` 中对该方法的调用

### 3. 添加 `get_mask_images` 方法

* 在 `LoadController` 中添加 `get_mask_images` 方法

* 实现该方法以获取 `data/cover/mask` 目录下的图片列表

* 确保返回格式与 `get_to_ps_images` 一致

### 4. 更新 API 路由

* 更新 `/api/get-mask-images` 路由，使用 `LoadController` 的 `get_to_ps_images` 方法

* 更新 `/api/get-crop-images` 路由，使用 `LoadController` 的 `get_mask_images` 方法

* 路由名字更新 第一个改为`/api/get_to_ps_images 第二个改为/api/get_mask_images`

### 5. 验证和测试

* 确保所有功能正常工作

* 确保 API 路由正确指向新的方法

* 确保蒙版和裁剪功能能够正常获取图片列表

